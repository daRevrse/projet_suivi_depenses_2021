import 'package:flutter/material.dart';

class solde extends StatefulWidget {
  const solde({Key? key}) : super(key: key);

  @override
  _solde createState() => _solde();
}

class _solde extends State<solde> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.grey,
      ),
    );
  }
}
